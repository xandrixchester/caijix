<div class="booyah-box col-10 offset-1">
	<h1>Enter Your Interest Details</h1>
	<%= simple_form_for @interest do |f| %>
		<%= f.input :title %>
		<%= f.input :description %>
		<br />
		<%= f.submit 'Create', class: 'btn btn-primary' %>
	<% end %>
</div>